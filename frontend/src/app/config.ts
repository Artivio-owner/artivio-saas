/**
 * ============================================
 * ARTIVIO — FRONTEND CONFIG
 * ============================================
 */

export const APP_CONFIG = {
  apiBaseUrl: import.meta.env.VITE_API_URL || '/api',
  appName: 'Artivio',
};